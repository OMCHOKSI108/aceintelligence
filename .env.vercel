# =============================================================================
# Vercel Environment Variables — copy these into Vercel Dashboard → Settings → Environment Variables
# Set for Production, Preview, and Development environments
# =============================================================================

# ── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://neondb_owner:npg_wN9eOWjr6LDE@ep-shy-queen-at1z0mv7-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require

# ── Auth ──────────────────────────────────────────────────────────────────────
# Generate a strong random string: openssl rand -base64 32
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# One-time seed route secret (generate a strong random string)
# Usage: curl -X POST https://aceintelligence.systems/api/admin/seed -H "x-seed-secret: YOUR_SECRET"
SEED_SECRET=change-me-to-random-string

# ── Email — Resend (candidate-facing) ────────────────────────────────────────
RESEND_API_KEY=re_L5ero3vk_NJ4yTGftFJg5Hjv4h8aVhNCW
EMAIL_FROM=aceintelligencejobs <noreply@aceintelligence.systems>

# ── Email — Gmail SMTP (admin internal) ──────────────────────────────────────
GMAIL_USER=omchoksi.pro@gmail.com
GMAIL_PASS=vydc zoiw oapo pqok

# ── Email — Contact form (separate Gmail SMTP credentials) ───────────────────
GMAIL_SMTP_EMAIL=omchoksi.pro@gmail.com
GMAIL_SMTP_PASS=cpmk dphx obwd fiuh

# ── Client URL (for email verification links) ────────────────────────────────
CLIENT_URL=https://aceintelligence.systems

# ── Public (client-side) ─────────────────────────────────────────────────────
NEXT_PUBLIC_CAREERS_FORM_URL=

# ── Botpress Chatbot (optional — remove if not using) ────────────────────────
NEXT_PUBLIC_BOTPRESS_INJECT_URL=https://cdn.botpress.cloud/webchat/v3.6/inject.js
NEXT_PUBLIC_BOTPRESS_CONFIG_URL=https://files.bpcontent.cloud/2026/07/04/10/20260704105748-65EUEE2Y.js
